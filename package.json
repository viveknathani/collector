{
  "name": "collector",
  "version": "0.1.0",
  "description": "get user contributions",
  "main": "index.js",
  "scripts": {
    "build": "npm run lint && npm run pretty && npx tsc",
    "start": "node build/index.js",
    "pretty": "npx prettier --write .",
    "lint": "npx eslint . --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github"
  ],
  "author": "viveknathani",
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.5.0",
    "@typescript-eslint/parser": "^5.5.0",
    "eslint": "^8.3.0",
    "prettier": "^2.5.0",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "node-fetch": "^3.1.0"
  }
}
